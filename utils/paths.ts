import * as Path from "path";

export const PROJECT_DIR = Path.join(__dirname, "..", "..", "..");

export const PAGES_DIR = Path.join(PROJECT_DIR, "pages");

export const NOTES_PAGES_DIR = Path.join(PAGES_DIR, "notes");
