import { NoteLayout } from "components/layout";

export const meta = {
  name: "Master Theorem",
  description: "Review and tools for Master Theorem",
  icon: "/icons/master-theorem.svg",
};

# Master Theory

Hello

export default ({ children }) => (
  <NoteLayout meta={meta}>{children}</NoteLayout>
);
