import { NoteLayout } from "components/layout";

export const meta = {
  name: "Real Induction",
  description: "Review and tools for Real Induction",
  icon: "/icons/real-induction.svg",
};

# Real Induction

hello

export default ({ children }) => (
  <NoteLayout meta={meta}>{children}</NoteLayout>
);
